

export default function UserPageLayout({
    children
}: {
    children: React.ReactNode;
}) {
    return (
        <section >
            
            {children}
      
        </section>
    );
}
