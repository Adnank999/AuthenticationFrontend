

export default function ManagerPageLayout({
    children
}: {
    children: React.ReactNode;
}) {
    return (
        <section >
            
            {children}
      
        </section>
    );
}
